# 전국부동산계산기

부동산 계산기 모음. 서버·DB·로그인 없이 브라우저에서만 계산하며 입력값을 저장하지 않습니다.

주소: https://전국부동산계산기.com

## 도메인

한글 도메인이라 기계가 읽는 곳에는 퓨니코드를 씁니다.

| 위치 | 값 |
| --- | --- |
| 사람이 보는 주소 | `전국부동산계산기.com` |
| 퓨니코드 | `xn--989anm2s84p8on6teba611m.com` |
| `CNAME` 파일, GitHub Pages 설정 | 퓨니코드 |
| `tools/build/site_core.py`의 `BASE` | 퓨니코드 (canonical·og:url·sitemap·JSON-LD가 여기서 나옴) |
| `js/common.js`의 `SITE_HOST` | 한글 (이미지 저장 문구처럼 사람이 보는 곳) |

DNS는 가비아에서 관리하며 A 레코드 4개(185.199.108~111.153)가 apex를, CNAME이 `www`를 `jhback-ctrl.github.io`로 보냅니다.

## 실행

`index.html`을 브라우저로 열거나 `python3 -m http.server 8080` 실행 후 http://localhost:8080 접속.

## 페이지

**계산기 21개**

| 파일 | 내용 |
| --- | --- |
| `rent.html` | 월세 실부담 — 관리비·보증금 기회비용 포함, 전월세 환산, 두 매물 비교, 누적 그래프 |
| `conversion.html` | 전월세 전환율 — 법정 전환율(기준금리+2%p, 상한 10%)과 시장 전환율 비교 |
| `rent-tax-credit.html` | 월세 세액공제 — 총급여 구간별 공제율과 연 1,000만원 한도 |
| `fee.html` | 중개보수 — 법정 상한 요율 기준 예상 최대 금액 |
| `loan.html` | 대출이자 — 원리금균등·원금균등·만기일시, 거치기간, 회차별 상환표 |
| `dsr.html` | DSR·LTV 한도 — 소득 기준과 담보 기준 한도 중 낮은 값, 스트레스 금리 반영 |
| `prepayment.html` | 중도상환수수료 — 잔여일수 비율, 면제일 |
| `acquisition-tax.html` | 취득세 — 취득세·지방교육세·농특세 합산, 다주택 중과, 생애최초 감면 (참고용) |
| `capital-gains-tax.html` | 양도소득세 — 비과세·고가주택 안분·장특공제·단기/다주택 중과·이월과세·부담부증여·감면 (참고용, 홈택스 확인 안내) |
| `yield.html` | 상가 수익률 — 공실·대출 반영 총투자·자기자본 수익률, 공실률 민감도 |
| `subscription.html` | 청약 가점 — 무주택기간·부양가족·통장기간 84점 |
| `area.html` | 평수 변환 — 평 ↔ ㎡, 평당·㎡당 가격, 참고표 |
| `renewal.html` | 계약 갱신 청구권 — 갱신 요구 가능 기간 D-day, 보증금·월세 5% 상한, 환산보증금 기준 |
| `jeonse-insurance.html` | 전세보증보험 자가진단 — HUG 요건(보증금 한도, 담보인정비율 90%, 선순위 60%, 신청 시기)별 판정 |
| `jeonse-fraud-check.html` | 전세 사기 위험 체크 — 전세가율 + 위험 신호 10개 가중치 점수 |
| `moving.html` | 이사 체크리스트 — 이사일 기준 단계별 할 일, 전입신고 14일·자동차 30일 기한, 진행률 |
| `rate-compare.html` | 고정 vs 변동금리 — 6개월 재산정 시나리오 총 이자 비교, 손익분기 연 변동폭 |
| `buy-vs-rent.html` | 매매 vs 전세 — 보유기간 총비용(이자·기회비용·세금·중개보수·가격변동) 비교, 손익분기 상승률 |
| `tax-calendar.html` | 세금 달력 — 취득세 60일, 등기 60일, 양도세 예정신고, 재산세·종부세·임대소득세 D-day |
| `salary.html` | 연봉 실수령액 — 4대보험·소득세·지방소득세 공제, 연봉별 표 |
| `severance.html` | 퇴직금 — 평균임금 기준 법정 퇴직금, 근속연수공제·환산급여공제 반영 퇴직소득세 |

**표·자료** `table-acquisition-tax.html`, `table-capital-gains-tax.html`, `table-brokerage-fee.html`, `table-subscription-points.html`, `base-rate-history.html` — 세율표·요율표·가점표·기준금리 이력. 값이 바뀌면 `tools/build/pages_tables.py`와 해당 js 상수를 함께 고친다.

**서식** `form-rent-receipt.html`(입력→인쇄), `form-notice.html`(통지문 예시), `form-special-terms.html`(특약 문구) — `tools/build/pages_forms.py`, `js/forms.js`

**용어 사전** `glossary.html` + `term-*.html` 39개 — `tools/build/pages_glossary.py`의 `TERMS`에 항목을 추가하면 페이지·목록·sitemap이 함께 생성된다.

**그 밖의 페이지**

| 파일 | 내용 |
| --- | --- |
| `index.html` | 홈. 카테고리별 계산기 목록과 안내 글. 옛 공유 링크(`?rent=…`)는 `rent.html`로 넘김 |
| `guides.html`, `guide-*.html` | 안내 글 4편 (상환 방식, 복비, 전월세 전환율, 전세 vs 월세) |
| `checklist.html`, `policy.html` | 양도·취득 체크리스트, 세제·정책 참고 (세액 계산 없음) |
| `about.html`, `privacy.html`, `terms.html`, `404.html` | 소개, 개인정보처리방침, 이용약관, 404 |

## 사이트 빌드

헤더·메뉴·푸터·메타 태그는 모든 페이지에 같은 틀로 들어갑니다. 페이지를 추가하거나 메뉴를 바꿀 때는
`tools/build/` 의 등록부를 고치고 `python3 tools/build/build.py` 를 실행해 다시 생성합니다. 자세한 내용은 `tools/build/README.md` 를 보세요.

## 재방문 기능

- **최근 계산 기억** — 각 계산기의 스위치를 켜면 입력값을 브라우저 로컬 스토리지에만 저장. 끄면 즉시 삭제. 기본 꺼짐
- **공유하기** — 기기 공유창(카카오톡 등)으로 결과 요약과 링크 전송. 미지원 브라우저는 복사로 대체
- **링크 복사** — 입력값을 URL 쿼리에 담은 링크 복사
- **이미지 저장** — 결과 카드를 PNG로 내려받기. 외부 라이브러리 없이 캔버스로 그림
- **테마 전환** — 헤더 버튼으로 밝게·어둡게 수동 전환. 선택은 로컬 스토리지에 저장
- **홈 화면 추가** — `manifest.json`과 `sw.js`. 정적 자산은 캐시 우선, HTML은 네트워크 우선

## 방문 통계

`js/analytics.js`의 `GA_ID` 또는 `NAVER_ID`에 측정 ID를 넣기 전까지는 아무것도 로드하지 않습니다.
ID를 넣으면 `privacy.html` 6-1 항목을 실제 도입 내용으로 갱신하세요.

## 상수 갱신 목록

바뀔 수 있는 값은 각 스크립트 상단에 모여 있습니다.

| 파일 | 값 | 확인처 |
| --- | --- | --- |
| `js/conversion.js` | 한국은행 기준금리 `BASE_RATE_DEFAULT`, 기준일 | 한국은행 |
| `js/fee.js` | 중개보수 상한 요율표 | 공인중개사법 시행규칙 별표 1 |
| `js/acquisition-tax.js` | 취득세율표, 생애최초 감면 한도 | 지방세법, 지방세특례제한법 |
| `js/capital-gains-tax.js` | 누진세율표, 단기·중과 세율, 중과 유예 종료일 `HEAVY_SUSPENDED_UNTIL`, 비과세 12억 기준, 장특공제표, 이월과세 기간, 감면율·한도 | 소득세법, 조세특례제한법. 매년 초와 개정 시 확인 |
| `js/rent-tax-credit.js` | 공제율, 총급여 기준, 월세 한도, 귀속연도 | 조세특례제한법 |
| `js/subscription.js` | 가점표 | 주택공급에 관한 규칙 별표 1 |
| `js/dsr.js` | LTV·DSR·스트레스 가산 기본값 | 금융위원회 발표 |
| `js/prepayment.js` | 수수료율·부과기간 기본값 | 각 금융기관 약정 |
| `js/renewal.js` | 갱신 요구 기간(6~2개월), 인상 상한 5%, 전환율 가산 | 주택임대차보호법 |
| `js/tax-calendar.js` | 취득세·양도세 신고 기한, 재산세·종부세 납부 기간 | 지방세법, 소득세법, 종부세법 |
| `js/jeonse-insurance.js` | 보증금 한도, 담보인정비율, 선순위 비율 | HUG 전세보증금반환보증 안내 |
| `js/salary.js` | 4대보험 요율, 국민연금 기준소득월액 상·하한, 근로소득공제·세액공제, 세율표 | 국민연금공단, 건보공단, 소득세법. 매년 초 확인 |
| `js/severance.js` | 근속연수공제·환산급여공제표, 세율표 | 소득세법 |
| `tools/build/pages_tables.py` | 표 페이지의 세율·요율·가점표, 기준금리 이력 `RATE_HISTORY` | 위 각 법령, 한국은행 |

## 구조

- `css/site.css` — 공통 스타일. 색·간격은 `:root` 변수로 관리하며 다크 모드 대응
- `js/common.js` — 숫자 파싱·포맷, 금액 입력 콤마, 토스트, 공유 링크, 내비
- `js/<계산기>.js` — 페이지별 계산 로직 21개. 공통 배선은 `Site.wireCalc`가 담당
- `js/analytics.js` — 방문 통계 로더 (ID 비어 있으면 동작 안 함)
- `sw.js`, `manifest.json` — 앱 설치와 오프라인 캐시
- `CNAME` — GitHub Pages 커스텀 도메인 (퓨니코드)
- `tools/build/` — 사이트 빌더
- `tools/make-og.js` — 공유 카드 이미지 `og.png` 생성 (`node tools/make-og.js`). 이름이나 문구를 바꾸면 다시 실행

기본 금리, 프리셋 값, 중개보수 요율표 등 바뀔 수 있는 값은 각 스크립트 상단에 상수로 모아두었습니다.

## 계산 공식

**월세 실부담**
- 월고정 = 월세 + 관리비 + 기타월고정
- 보증금월기회비용 = 보증금 × (연금리% ÷ 12 ÷ 100)
- 실부담월 = 월고정 + 보증금월기회비용, 실부담연 = 실부담월 × 12
- 전세→월세상당 = 전세보증금 × (전세연금리% ÷ 12 ÷ 100)
- 월세→필요전세금 = max(0, (실부담월 − 관리비 − 기타) ÷ (전세연금리% ÷ 12 ÷ 100)), 분모 0이면 미표시

**대출이자**
- 원리금균등 월납 = P × r × (1+r)^n ÷ ((1+r)^n − 1), r = 연금리 ÷ 12 ÷ 100
- 원금균등 = 매월 원금 P÷n + 잔액 × r
- 만기일시 = 매월 P × r, 만기에 원금 일시 상환
- 거치기간 중에는 이자만 납부하며 원금 상환은 거치 후 회차부터

**상가 수익률**
- 연 순영업수익 = 월세 × 12 − 공실 차감 − 연 운영비
- 실투자금 = 매매가 + 취득 부대비용 − 임대 보증금
- 총투자 수익률 = 순영업수익 ÷ 실투자금
- 자기자본 수익률 = (순영업수익 − 연 대출이자) ÷ (실투자금 − 대출금액)

**중개보수**
- 주택: 거래금액 구간별 상한 요율과 한도액 적용
- 월세 거래금액 = 보증금 + 월세 × 100, 환산액이 5천만원 미만이면 보증금 + 월세 × 70
- 주거용 오피스텔 매매 0.5% / 임대차 0.4%, 그 외 0.9% 이내 협의
- 부가세 10%는 선택 표시

## 공통 기능

- 입력 즉시 재계산. CTA 버튼은 재계산 후 결과로 스크롤
- 금액 입력 천단위 콤마, 금리·기간 0·빈값·음수 가드
- 예시 프리셋 버튼, 결과 링크 복사(입력값을 URL 쿼리에만 담음), 모바일 하단 요약 바
- 한글 UI, 접근 가능한 라벨, 반응형, 다크 모드

본 계산기는 참고용이며 금융·세무·법률 자문이 아닙니다.
